/*!
 * setprototypeof
 * Copyright(c) 2015-2018 Fedor Indutny
 * MIT Licensed
 */

'use strict';

/**
 * Module exports.
 * @public
 */

module.exports = Object.setPrototypeOf || ({ __proto__: [] } instanceof Array ? setProtoOf : mixinProperties);

/**
 * setProtoOf - Sets the prototype of one object to another.
 * 
 * @param {Object} obj - The object to modify.
 * @param {Object} proto - The prototype to apply.
 * @returns {Object} The modified object.
 * @private
 */
function setProtoOf(obj, proto) {
  obj.__proto__ = proto;
  return obj;
}

/**
 * mixinProperties - A fallback implementation of setPrototypeOf that copies
 * all properties from the prototype to the object.
 * 
 * @param {Object} obj - The object to modify.
 * @param {Object} proto - The prototype to copy properties from.
 * @returns {Object} The modified object.
 * @private
 */
function mixinProperties(obj, proto) {
  for (var prop in proto) {
    if (!Object.prototype.hasOwnProperty.call(obj, prop)) {
      obj[prop] = proto[prop];
    }
  }
  return obj;
}